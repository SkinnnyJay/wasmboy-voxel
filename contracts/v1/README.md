# Contracts v1

Versioned API contract documentation lives here for migration tracking.

Runtime schemas and TypeScript types are implemented in:

- `packages/api/src/contracts/v1/*`

This folder is the version anchor used by the migration plan and CI contract checks.
