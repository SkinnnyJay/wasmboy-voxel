# Baseline Snapshot Artifacts

Generated by:

```bash
node --experimental-worker test/integration/capture-baseline-snapshots.js
```

Artifacts:

- `back-to-color.snapshot.json`
- `tobutobugirl.snapshot.json`
- `summary.json`

`summary.json` includes:

- per-ROM snapshot read timing
- per-ROM execution timing used before capture
- SHA-256 checksums for tile data and OAM

These files are migration baselines for Milestone 0 tasks:

- capture baseline snapshot JSON
- record baseline VRAM/OAM checksums
- record baseline timing metrics
