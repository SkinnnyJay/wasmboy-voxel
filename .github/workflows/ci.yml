name: ci

on:
  workflow_dispatch:
    inputs:
      strict:
        description: 'Run strict no-retry quality gate variant'
        required: false
        type: boolean
        default: false
  push:
    branches: ['master', 'cursor/**']
    paths:
      - 'package.json'
      - 'package-lock.json'
      - 'core/**'
      - 'lib/**'
      - 'packages/**'
      - 'apps/**'
      - 'contracts/**'
      - 'test/**'
      - 'scripts/**'
      - 'index.ts'
      - 'voxel-wrapper.ts'
      - 'fork-api-spec.ts'
      - '.changeset/**'
      - '.github/workflows/*.yml'
  pull_request:
    branches: ['master']
    paths:
      - 'package.json'
      - 'package-lock.json'
      - 'core/**'
      - 'lib/**'
      - 'packages/**'
      - 'apps/**'
      - 'contracts/**'
      - 'test/**'
      - 'scripts/**'
      - 'index.ts'
      - 'voxel-wrapper.ts'
      - 'fork-api-spec.ts'
      - '.changeset/**'
      - '.github/workflows/*.yml'

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-typecheck-test:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: |
            package-lock.json
            packages/api/package-lock.json
            packages/cli/package-lock.json
            apps/debugger/package-lock.json

      - name: Cache Next.js build cache
        uses: actions/cache@v4
        with:
          path: |
            apps/debugger/.next/cache
          key: ${{ runner.os }}-next-cache-${{ hashFiles('apps/debugger/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-next-cache-

      - name: Install workspace dependencies
        run: npm run install:stack:ci

      - name: Run full no-build quality suite
        run: |
          set -o pipefail
          if [[ "${{ github.event_name }}" == "workflow_dispatch" && "${{ inputs.strict }}" == "true" ]]; then
            npm run ci:local:strict 2>&1 | tee ci-quality.log
          else
            npm run ci:local 2>&1 | tee ci-quality.log
          fi

      - name: Compress diagnostics payload
        if: ${{ failure() || cancelled() }}
        run: |
          mkdir -p artifacts
          diagnostic_files=()
          shopt -s nullglob
          for file in ci-quality.log test/core/save-state/*.png test/integration/headless-simple*.png; do
            if [[ -f "$file" ]]; then
              diagnostic_files+=("$file")
            fi
          done
          shopt -u nullglob

          if (( ${#diagnostic_files[@]} > 0 )); then
            tar -czf artifacts/ci-diagnostics.tar.gz "${diagnostic_files[@]}"
          else
            echo "No diagnostics files were produced for this run." > artifacts/ci-diagnostics.txt
            tar -czf artifacts/ci-diagnostics.tar.gz artifacts/ci-diagnostics.txt
          fi

      - name: Upload failure diagnostics
        if: ${{ failure() || cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: ci-failure-diagnostics-${{ github.run_id }}-${{ github.run_attempt }}
          retention-days: 14
          if-no-files-found: ignore
          path: |
            artifacts/ci-diagnostics.tar.gz
